Vagrant::Config.run do |config|
  config.vm.box = "base"

  # IP address
  config.vm.network "44.44.44.44"

  # Port forwarding
  config.vm.forward_port("ssh", 22, 2222)
  config.vm.forward_port("cast_agent", 49443, 49443)
end
